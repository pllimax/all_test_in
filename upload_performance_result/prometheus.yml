global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "sglang_benchmark"
    static_configs:
      - targets: ["host.docker.internal:9099"]
        labels:
          service: "sglang-benchmark"
